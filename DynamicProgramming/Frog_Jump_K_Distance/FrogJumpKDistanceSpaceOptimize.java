package Frog_Jump_K_Distance;

public class FrogJumpKDistanceSpaceOptimize {
    // Not required because we have to store k size list and what if k = n. Then we have to take n size list which is again O(n). So space optimization is not required
}
